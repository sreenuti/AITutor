import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sahasra Geometry Tutor - 8th Grade Geometry Learning",
  description: "An AI-powered geometry tutoring app for 8th grade students. Upload worksheets, get explanations, practice problems, and track your progress.",
  keywords: ["geometry", "tutoring", "8th grade", "education", "learning", "mathematics"],
  authors: [{ name: "Sahasra Geometry Tutor" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
